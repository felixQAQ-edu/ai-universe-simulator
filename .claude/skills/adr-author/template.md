# ADR-NNN · <一句话决策主题>

- **日期**:YYYY-MM-DD
- **状态**:已采纳
- **决策者**:Felix

## 背景

<为什么现在要做这个决策。给出当前进度上下文(M? · Week? · Day?)、触发这次决策的具体问题、相关的 SPI / 框架语义 / 前序 ADR。>

<若有约束,列成编号清单:>

约束条件:

1. <约束一,例:M1 阶段只用关系数据库,向量库要到 M6 才引入>
2. <约束二>

## 候选方案

### 方案 A:<简称>

**优点**:
- <…>

**缺点**:
- <…>

### 方案 B:<简称>

**优点**:
- <…>

**缺点**:
- <…>

### 方案 C:<简称>(本 ADR 采纳)

<被采纳的方案在标题后标注「(本 ADR 采纳)」。>

**优点**:
- <…>

**缺点**:
- <接受这一权衡的说明>

## 最终决策

**方案 X — <简称>**,<必要的配置 / 实现细节。可按需拆子小节,例如:>

### 1. <表结构 / 接口 / 配置项>

<代码块、SQL、字段语义表等>

### 2. <实现策略>

<伪代码 + 关键理由>

### 关键理由

1. <为什么这是对的选择,呼应约束与项目原则>
2. <复用了哪些已建立的模式 / 肌肉记忆>
3. <正确避开了哪个常见陷阱>
4. <保留了哪些未来演进路径>

## 已知代价

<诚实列出被接受的权衡;能写缓解方式就写。这是本项目 ADR 的灵魂。>

1. **<代价一>**:<说明>。缓解方式:<…>
2. **<代价二>**:<说明>。
3. **<被排除方案的代价>**:<为什么仍接受>。

## 重新审视的触发条件

<让决策可被未来推翻,尽量挂到里程碑或客观信号。>

- <例:M5 压测 50 并发下发现 X 成为瓶颈>
- <例:依赖库升级到 2.x,默认行为变化>
- <例:引入 Y 新场景 / 新需求>

## 实施步骤

<可含 bash 命令块与验证步骤;或用 ✅ Step N 勾选清单记录进度。>

1. <步骤一>
   ```bash
   <命令>
   ```
2. **IDE 全文搜确认无残留引用**(全局变更前置纪律):<…>
3. <验证:mvn test 预期全绿 / 真实 curl 验证 / 日志与落库证据>
4. <commit + push,附清晰 commit message>

## 实际效果(事后补充)

*<里程碑 / 场景>时回填:<要用真实数据验证什么>。*

## 跟其他文档的交叉引用

- **起源 / 经验**:LEARNING-NOTES 笔记 <N>(<一句话>)
- **框架 / 选型基础**:ADR-<NNN>(<一句话>)
- **配套防御 / 相关源文件**:`<path>`(<一句话>)
