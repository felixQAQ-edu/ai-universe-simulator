# ADR-020 刀 4 ·《寻常》真 key 冒烟清单

> **这一刀的性质**:前三刀全是「写进去」——registry 四轴(刀 1)、world-gen 世界观与结局池(刀 2)、
> event-loop 逐回合写作标准七件(刀 3)。三刀没有任何一条被真 key 验过:**单元测试能证明指令进了
> prompt,证明不了模型照做**。本刀验的就是这个差。
>
> **零代码**。发现故障**先报不修**,由 Claude 判是 prompt 层还是结构层 —— 尤其 §4 若不过,
> 那是「软自律守不住」的实证,触发 ADR-020「重新审视的触发条件」第一条,**是另一刀不是补丁**。

---

## 〇、开玩前必读:三条会让读数变形的事(先说清,免得把「过了」当证据)

### 0.1 回收机制没有记忆载体 —— 影响第 2 条与观察项 A / B 的判读

`Engine.compressLog()`(`LOG_KEEP = 4`)把超出 4 回合的旧回合折成 **`[T7选B] [T8选A]`** ——
**只有回合号和选项字母,叙事内容一个字不留**。第 45 回合时模型对第 1–41 回合的全部记忆 =
这串字母 + 一句 AI 自维护的 `timeline` + 最近 4 回合原文。

而《寻常》worldview 写死的世界引擎是【回收】(「早期那些不起眼的东西,几十年后会变形回来」)。
**跨 4 回合以上的回收在结构上做不到**,除非模型一直把那件东西驮在 `timeline` 那一句里。
`logSummary` 剧情摘要折叠一直是 ROADMAP 上的候补刀(动 Engine 需重录 golden),
**《寻常》是第一个真正依赖它的世界**。

**本刀不修**(Felix 裁定 2026-08-10)。但冒烟目标随之增加一条,而且是**本刀最重要的一条** ——
见 [§三 A](#a--长程记忆缺失下寻常还成不成立本刀最重要的一条):
在没有长程记忆的条件下,《寻常》读起来还成不成立?
**那条的判据不是过 / 不过,是结论指向哪一刀**(logSummary 维持候补 / 升为《寻常》的前置)。

据此调整判读:

- **第 2 条可能「因为忘了所以过了」** —— 判过时必须同时记「它有没有被再提起过」。
  只有**被提起、且没被写实**才算真守住;从头到尾没再出现 → 记 `未复现`,不记 `过`。
- **观察项 B(埋几处 / 中几处)预期 0–1 中**,且命中大概率落在 4 回合窗口内 ——
  那不是「几十年后回收」,是「刚才的事」,记的时候要分开。
- world-gen 槽那条「结局可以回收一条」**大概率无料可回收**。

### 0.2 选项条数按实际数,不要按 4 算 —— 这是真实口径冲突,冒烟后要处置

骨架写的是 `availableActions:2-4 个`,而刀 3 槽里的铁律 3(「四个选项里至少有一个是最常见的
那种做法」)与 §4 判据(「每个回合的四个选项」)都写死了**四个**。
**两处口径在仓库里本来就打架,不只是数法问题** —— 铁律 3 与 §4 判据在只有 2–3 个选项的回合上
字面失效。

- 数的时候按每回合**实际条数**数(记 `5 回合 / 共 N 个选项`);
- **记下 3 选项(或 2 选项)回合的出现频率**:`___ / ___ 回合`,并记它们分布在哪一段
  (年轻密集段 / 中段 / 收束段)。

**冒烟后处置**:频率是判断往哪边收口的唯一依据 ——
频率极低(个位数百分比)→ 收紧槽内措辞即可;
频率可观 → 说明该改的是「四个」这个假设本身,而不是让模型去迁就它。
**本刀只记数,不改任何一处措辞。**

### 0.3 顶部会有一条空白带 —— 是裁定一的已知代价,不是故障

《寻常》未登记皮肤 → `sceneUrl = null`,不渲染图层;但 `.banner` 高度写死
`clamp(300px, 42vh, 380px)`,所以每回合顶部有 300–380px 的空渐变带,正文被推下去。
**这是裁定一「先验玩法再投视觉成本」的代价,不判过不过**,但请记一句真机体感
(在手机上这条带子有多碍事)。

### 0.4 时间与钱的预期

一局 40–55 回合 × 每回合约 15s 生成 + 阅读 → **单局约 1–1.5 小时**。
成本:单回合约 ¥0.0035 → 一局约 ¥0.2,加 world-gen 那一发胖调用,**三局仍在 ¥1 以内**
(日闸 ¥6 / init 10 次 / 回合 300 次,均够用)。

---

## 一、部署与前置检查(Felix 亲手)

在仓库根目录、`main` 上执行:

```bash
fly deploy --build-arg GIT_SHA=$(git rev-parse --short HEAD)
```

部署完先验镜像真换了(runbook §3.1.5,**不做这步后面全白验**):

```bash
curl -s https://wanjie-ai.fly.dev/actuator/info
```

```bash
git rev-parse --short HEAD
```

- **两者相等** → 往下走。
- **不等** → 先跑 `git log <线上SHA>..HEAD --stat`;若差的 commit **只动 `docs/`** 则代码一致、
  照常往下;否则是真陈旧,重新 deploy。
- **`unknown` 或带 `-dirty`** → 没传 build-arg 或工作区不干净,重来。

再确认线上不是 mock(runbook §3.1.6 —— **只设 key 不设 active = 线上跑 mock,而 mock 不读 key,
所以「key 设了」不构成任何证据**):

```bash
fly secrets list -a wanjie-ai
```

- 须同时有 `AIUNIVERSE_LLM_ACTIVE` 与 key,状态皆 Deployed。
- 最终证据看叙事本身:mock 是固定桩文本,一眼可分。

**开局**:选择屏应出现第五张卡「人生模拟」,**可点**(不再是灰的「敬请期待」),
tagline「一生只有一次,而它大部分时候都很寻常。」

---

## 二、必验四条

> 逐条勾。**过 / 不过 / 无法验** 三选一 —— 「无法验」不是失败,但不许写成「过」。

### 第 1 条 · §4 退化判据(唯一承重点)

**前置**:热望归零(数值面板显示 `热望 0 · 熄了`)。

前置有三种结局,都要记:

- [ ] **归零,且归零后还剩 ≥5 个回合** → 正常验,往下走。
- [ ] **整局没归零**(记录全局最低值 `____`)→ 记 `无法验 · 未归零`。
      **这本身是发现**:说明降幅设计不足、归零几乎不可达。
- [ ] **归零太晚,剩余回合 < 5**(归零在第 `___` 回合,收束在第 `___` 回合)→ 记 `无法验 · 归零过晚`。
      **这同样是发现**,而且是更隐蔽的那种:判据写的是「连续 5 个回合」,而热望最低的时候
      按叙事本能就是暮年 —— 若这条反复出现,说明判据的可数形式与世界的时间结构对不上。

**验法(前置满足时)**:从归零后第一个回合起,**连续数满 5 个回合**,每回合逐个看每个选项,
数有没有引入 **新的人 / 新的地点 / 新的时间约定**。

> ⚠️ **不许看两个回合觉得像就过。满 5 个,逐条数。**
> 只数**选项文本**(叙事里出现新的人不算)。「新」= 此前从未出现过的那个人 / 那个地方。

| 回合 | 选项数 | A | B | C | D | 有无引入 |
|---|---|---|---|---|---|---|
| 第 ___ 回合 | ___ | | | | | |
| 第 ___ 回合 | ___ | | | | | |
| 第 ___ 回合 | ___ | | | | | |
| 第 ___ 回合 | ___ | | | | | |
| 第 ___ 回合 | ___ | | | | | |

- [ ] **过**:5 个回合共 ___ 个选项,**零引入**。
- [ ] **不过**:第 ___ 回合第 ___ 个选项,引入的是 **人 / 地点 / 时间约定**(圈一个),原文抄下:
      `____________________`。

**顺带记(不判过不过)**:路口若也归零,记它归零后的选项形态 ——
应当**仍是四个真实动作、只是没有一个会改变什么**(§5)。
若写成了发呆 / 放弃 / 事务待办清单(「处理房子」「办丧事」),记下来:
那是刀 3 槽里明令禁止的写法,也是 ADR-020 §5 就地订正掉的那个错误口径复活。

### 第 2 条 · §6 三处留白

逐处记。**注意 §0.1:没被再提起 ≠ 守住了。**

| 留白 | 是否出现过 | 出现后是否被写实 / 复述 | 判读 |
|---|---|---|---|
| 旧手机「备注的两个字」 | 出现 / 未出现 | | |
| 「给谁打个电话」的那个谁 | 出现 / 未出现 | | |
| 母亲的口头禅 | 出现 / 未出现 | | |

- [ ] **过**:三处中**出现过的那些**,内容一律未被写实。
- [ ] **不过**:第 ___ 处被写实,原文:`____________________`。
- [ ] **未复现**:三处从头到尾都没出现 → 不记过,记 `无法验`,并与 §0.1 一起报。

**口头禅那条最难验,单列**:它应当在**埋点回合出现一次**、**回收回合原样再出现一次**,
中间任何一次系统主动提起都算不过。

- 埋点回合:第 ___ 回合;原文 `____________________`
- 中间被提起:**有 / 无**(有 → 第 ___ 回合,**不过**)
- 回收回合:第 ___ 回合;是否**原样**:**是 / 否**;是否加了说明或点破:**是 / 否**(是 → 不过)

### 第 3 条 · 密度与不显岁数

- [ ] **回合总数**:实际 ___ 回合(过 = 40–55)。
      **低于 40 的风险大于高于 55** —— 槽里那张六段密度表加起来约 40 回合,正压在下沿。
- [ ] **全程没有出现过岁数数字**。
      不过的样子:「你 32 岁那年」「三十五岁」「第 20 回合」「五年后」这类跨度说明。
      出现即记第 ___ 回合、原文 `____________________`。
- [ ] **每回合第一句落在一个带年代刻度的物上**(饭盒 / 报名表 / 第一份工资 / 电视音量 / 换手机…)。
      抽查不算 —— 至少连看 10 个回合,记有几个是抽象处境句开头(`___ / 10`)。
- [ ] **年轻段一年一回合的实感成立**(19–30 岁那段:是不是一回合一件具体的事,
      而不是一回合跳过好几年)。

### 第 4 条 · §8 收束气力下限(本刀最需要抓的故障)

**这条不会报错**。gate 改判只往 `issues` 记一行,玩家侧和我们都看不见 ——
所以要靠**叙事与结局标签对不上**来抓。

- [ ] **最后 3–5 回合气力 ≥ 15**:逐回合记 `第 ___ 回合气力 ___`(至少记末 5 个)。
- [ ] **结局是「寿终」类**(寿终·圆满 / 寿终·有憾 / 寿终·无人),而非【早逝】。
- [ ] **叙事与结局标签一致**。

**不过的样子(必须抓的那个)**:
> 结局文字在讲一辈子走完了、该在的人还在,**但结局落的是 failure / 早逝类** ——
> 那就是 gate 静默改判(致命轴 ≤10 且 AI 提了 success → 引擎拒绝改判失败)。

出现即记:末回合气力 `___`、结局标题 `____________`、结局正文头一句 `____________________`。

**另一种不过(不触 gate 但仍不合格)**:末回合气力落在 11–14 —— gate 不响,但违反槽里那条
「不得低于 15」。这条只能靠逐回合抄数值抓,别省。

---

## 三、观察项(只记数,不判过不过)

> A 是本刀最重要的一条,**它的结论指向下一刀是什么**;B–F 只记数。

### A · 长程记忆缺失下,《寻常》还成不成立(本刀最重要的一条)

**为什么问这个**:§0.1 已实证回收机制没有记忆载体,而**本刀不修**。
那么真正要回答的就不是「回收做得好不好」,而是更靠前的一个问题 ——
**在没有长程记忆的条件下,这个世界读起来还成不成立?**
《寻常》没有守则、没有资源经济、没有敌人,它全部的重量压在「一辈子」这个整体感上;
如果那个整体感靠 4 回合窗口撑不住,那 logSummary 就不是候补刀,而是这个世界的地基。

一局玩完后回答三条(**凭读完的整体印象,不要回翻记录**):

- **有没有任何一处让你停一下的瞬间**:**有 / 无**
  - 若有:第 ___ 回合前后,是什么让你停的:`____________________`
  - 有几处:___ 处
- **它读起来更像哪一个**(圈一个,并写一句为什么):
  - [ ] 一个人的一生
  - [ ] 一串互不相关的片段
  - 为什么:`____________________`
- **最近 4 回合窗口内的呼应,有没有产生类似回收的效果**:**有 / 无 / 说不清**
  - 若有,举一处:`____________________`
  - 这一条问的是:短程呼应能不能**部分替代**长程回收(能 → 世界靠窗口也立得住;
    不能 → 只有长程回收才产生那个效果)。

**判据不是过 / 不过,是结论指向哪一刀**:

| 读感 | 结论 |
|---|---|
| **读起来仍成立**(像一个人的一生,有停顿的瞬间) | `logSummary` 那一刀**维持候补**,《寻常》可继续往前走 |
| **明显是流水账**(互不相关的片段、全程没有一处让你停) | `logSummary` **是《寻常》的前置,不是候补** → **升为下一刀** |

⚠️ **中间态要如实记,不要往两边靠**:若读感是「像一生,但那个整体感来自玩家自己脑补而不是
文本给的」,原样写下来 —— 那会指向第三种处置(先补 `timeline` 的用法,而非直接动 Engine)。

### B · 回收:埋了几处 / 中了几处

- 埋(能认出是有意埋的具体物 / 具体一句话):约 ___ 处
- 中(后来真的变形回来了):___ 处
- 其中**跨 4 回合以上**的:___ 处 ←— **这一栏是 §0.1 的直接读数**

判读:预期 0–1 中且多为窗口内命中。
**连续三局都命中五处以上 → 说明命中率偏高**(写作标准 §三 要的是「埋七八处、中三四处」),
那是发现不是故障。

### C · 结局是否回收了一条,是否以「别人不知道」的方式

- 结局是否回收了早期某个具体的物 / 某一句话:**是 / 否**
- 若是:是否「东西还在、人不在了,认得它的只有你和玩家」:**是 / 否**
- 是否被解释 / 点破 / 列成清单:**是 / 否**(是 → 记下来,那是写作标准 §三 明令禁止的)

### D · 起局种子的不公平感是否成立(需 ≥2 局对比)

四条种子刻意不等价(1978 西北农场父亲早亡 `high` / 1985 北方双职工 `low` /
1992 南方县城 `medium` / 2001 沿海城中村 `medium`)。

- 局 1 种子:`____________` 危险等级 `____`,体感 `____________`
- 局 2 种子:`____________` 危险等级 `____`,体感 `____________`
- **不公平感成立**(开局明显不一样难):**是 / 否**
- **难牌局仍能通向圆满**(FAIR_START 硬约束):**是 / 否 / 本次未验到**
  ——「好牌局的圆满打个折」是被明令禁止的,若难牌局的圆满读起来像安慰奖,记下来。

### E · 默认呈现路径(裁定一)

- [ ] 无顶图不塌、四轴面板显示正常(气力 / 热望 / 路口 / 牵挂 + 档名)
- [ ] 无控制台报错
- [ ] 顶部空白带的真机体感:`____________________`(§0.3,只记不判)

---

## 四、结论

> 由 **Felix 拍板**,不以任何模拟结果或本地读数覆盖。

- 必验四条:1 `___` / 2 `___` / 3 `___` / 4 `___`(过 / 不过 / 无法验)
- 总判定:**通过 / 不通过**
- 若通过 → ROADMAP 记结论;**CONTEXT §三.16 (7) 扩 per-archetype 的挂账可解冻**
  (ADR-020 §1 挂账,循「待验通再立字」先例)。
- 若不通过 → 报上来,**不自行修**。
  尤其:**§4 不过 = 软自律守不住的实证** → 触发 ADR-020「重新审视的触发条件」第一条,
  评估把「选项退化」做成结构约束,**那是另一刀不是补丁**。

### 观察项 A 的结论(与总判定独立,单独记)

**《寻常》在无长程记忆下是否成立**:成立 / 明显是流水账 / 中间态

- 成立 → `logSummary` 剧情摘要折叠**维持候补**。
- 流水账 → `logSummary` **升为下一刀**(它是《寻常》的前置,不是候补)。
- 中间态 → 记原话,评估第三条路(先补 `timeline` 的用法,不动 Engine、不重录 golden)。

⚠️ **这一条与必验四条的总判定互不覆盖**:四条全过而 A 判流水账,《寻常》仍不能算做完;
四条里有「无法验」而 A 判成立,也不必因此把 logSummary 提前。

### 待处置(冒烟后,不在本刀)

- **§0.2 口径冲突**(骨架 `2-4 个` vs 槽内「四个选项」):3 选项回合频率 `___ / ___ 回合`
  → 按 §0.2 的两档判据决定往哪边收口。
