# Week 3 复盘(2026-06-29 ~ 2026-07-05)

> 从 `docs/ROADMAP.md` 周度日志归档(「最近三周,循环覆盖」滚动,2026-07-20 移出)。原文逐字保留。

- **完成(Phase 2 打磨期 scoping 定稿,纯 docs,2026-06-30)**:五模式可玩后进打磨期,玩家视角十条改进反馈(自评 demo→可连续游玩的差距)经 Project 窗口一次 consolidation **坍缩成四块主题 + 分档顺序**,结论固化进仓库。**四块主题**:A·持久世界记忆(#2/5/6/9/10,价值极高成本极高)、B·让数值与选择有重量(#1/3/4)、C·意外与反转(#7,依赖 A 故薄)、D·每世界氛围图(#8)。**本轮范围**=#3 数值行为化(轴元数据加 3 档阈值→行为/叙事,挂 ADR-009 `axisRole`)+ #8 静态氛围图(每世界一张人工审过的静态底图,守静态侧、不漂向运行时生成)+ #1 选择反馈(定性版,AI 标选项风险/代价提示当叙事元数据、引擎只读,**先不做真概率掷骰**;需一条小 ADR),目标「把四个世界做有重量」。**岔路拍板**=十条反馈实质在提一个与原定杀手锏不同的杀手锏(个人史/AI 记住玩家的一生)→ **押混合模式**(与 §七「旗舰模式=最大差异点」既有定位一致),**个人史/持久世界记忆作为有意识权衡后的 runner-up 冻结入档**(非遗忘、是择序;地基压在尚未做的账号+云存档,重启先补基础设施)。**纯 docs**:新建 [`docs/phase2-polish-and-vision-backlog.md`](phase2-polish-and-vision-backlog.md)(逐字落盘成稿)+ 回填旧 [`phase1-playability-backlog-and-polish.md`](phase1-playability-backlog-and-polish.md) 推迟项 ②配图/③b难度 指针(只加指针不删原文)。**不新增 ADR**(无实现决策:混合模式 ADR 待其启动时起、#1 小 ADR 待开 #1 时起)、**`schemaVersion`/CONTEXT 不动**。§七「旗舰模式=混合模式=最大差异点」一行确认保留无需改(岔路结论与之一致)。
- **完成(#3 数值行为化(descriptive)落地 + 真 key 冒烟验通,2026-06-30)= 打磨期第一刀**:打磨期开第一刀挑了最便宜马上见效的 #3。给四世界(规则怪谈/末日/克苏鲁/修仙)各数值轴配三档「行为档」`bands`,把**当前档**染进**叙事 + 前端展示**让世界「有重量」。**已拍板子决策=只染叙事(descriptive)、绝不 gate 选项**(gating=#4,推迟到状态层——这是承重接缝,提示词明文禁「据档位开关可选行动」)。**架构站位**:`bands` 是 ADR-009 `AttributeAxis` 元数据扩展,**引擎一行不动、不读它、AI/前端只读不裁决**(守 ADR-008);摸代码确认轴元数据下发走 **API DTO**(`InitResponse.attributes`)非被校验 wire schema → **`schemaVersion` 不动(仍 "0.4")**(无需如 ADR-008 key-agnostic 那次停下报)。**三切片各 atomic commit**(A 纯数据→B 提示词→C 前端):**A**(`2bc6016`)`AttributeAxis.Band`(threshold/label/narrationHint)+ `withBands` + 纯函数 `resolveBand`(**axisRole 感知**:depletion threshold=上界/降序进带、accumulation=下界/升序进带)+ 构造器良构校验(阈值单调/不重/在域内 + 覆盖顶/底档 + label 非空)+ registry 四世界配档(阈值 depletion 50/20、accumulation 30/60,**Felix enumerate 后签字**)+ init DTO 下发;**B**(`6c375c0`)event-loop 每回合按当前绝对值算当前档、注入**当前档** label+narrationHint(**只送当前档不送整表**守成本)+ SYSTEM 加「叙事须贴合状态档」(濒危=脚步虚浮、灵力枯竭=力竭非伤身…,在 A-1 既有 2–4 句内化入不额外加长),lockstep `prompts/event-loop.md` v0.8 + 运行时副本 `TurnPromptBuilder.SKELETON` + **lockstep 守护测试**(防只改一边漂移);**C**(`75c9ed3`)前端数字旁显示当前档 label(「气血 28 · 受创」,守 ADR-003 纯展示),DTO 行为档投影精炼为**显式 inclusive 区间 `{min,max,label}`**(`AttributeAxis.bandRanges()` 据 axisRole 算、连续覆盖 [0,100])——前端 **role-agnostic**(只需 `min≤value≤max`、免下发 axisRole 给展示层),`narrationHint` 不下发前端(仅服务端注入 prompt,守消毒)。`mvn test` **212 绿**(188 baseline + 24)、前端 `npm test` **36 绿**(30 + 6)+ lint/build 全绿、**golden parity 字节级零回归**(Engine 未动)。**真 key 冒烟 Felix 浏览器验通**:克苏鲁实测档位标签随值正确切换、叙事贴合当前档、未顶爆 A-1、无泄露,四世界文案按草稿验收 → CONTEXT **v1.2**(§三.14 加 `bands` 字段说明)。**走 b 方案(先合后验)**:Felix 不熟切分支,3 commit **ff 合并 `main`**(`c962af8..75c9ed3`,干净 ff 无 merge commit)后在 main 冒烟,**未 push、待 Felix 点头**。**末日「饥饿」轴改名**(displayName 高值=不饿、与值方向反直觉)因「饥饿」已 woven-in prompt/condition 文案(AXIS_IMAGERY/world-gen 示例/condition「饥饿而亡」),改 displayName 会两套叫法 → 拆**独立小单元**入 [打磨与愿景 backlog §6](phase2-polish-and-vision-backlog.md)、本轮未动。
- **完成(#8 顶部氛围图接入面只读勘察 + 三条衍生候选入 backlog 冻结,纯 docs,2026-06-30)**:为定 #8 顶部氛围图怎么接,先做只读勘察盘清 turn/init 契约与前端顶部栏取数 + 静态资源约定。**关键结论**:每回合 turn(`TurnDelta`/`buildDelta`)只返回 `turn/status/数值/已发现规则/可选动作`;**标题/危险度/基调全是 init 一次性生成、整局静态**(`world.world.title`/`dangerLevel`/`tone`),**无时间/天气字段**(截图里若有=AI 散文文字,无结构化来源);世界标识 = `world.archetypes[0]`(init 就有、整局稳定,#8 选图最佳键);AI 每回合产出的 `state.timeline` 引擎已存但 `buildDelta` 未下发。静态图约定:`web/public/`(Vite 默认 publicDir,现仅 favicon.svg)放图 → 构建挂根路径 `/scenes/<archetype>.webp`,前端按 archetype 选图 + CSS background,**后端零改**(#8 第 1 档静态底图可纯前端落)。**由勘察引出三条「让封面随世界演变」候选,均依赖已冻结的主题 A(持久世界记忆)、非 #8 纯前端范围 → 入 [打磨与愿景 backlog §7](phase2-polish-and-vision-backlog.md) 冻结**:① timeline 下发(最低成本踏板,动 turn 契约非纯前端)② 换地点封面(每地点一张图 + 地点状态机,触 1.5 档以上)③ 细节随回合演变(同场景多损坏度图集/逐回合生成,主题 A 核心果实、最贵)。纯文档,不新增 ADR、`schemaVersion`/CONTEXT 不动。
- **完成(#8 顶部氛围图静态第 1 档落地 + Felix 冒烟验通,2026-07-01)= 打磨期第二刀**:勘察确认可纯前端落后接图。**功能**:四世界游戏顶部一条 establishing-shot 氛围带——据 `world.archetypes[0]` 选静态锚图铺顶 + 图底 `linear-gradient` 融进正文白底(顺带盖住锚图右下角水印)+ 其上叠现成状态字段(回合 / 危险度中文 / 标题 / tone 副标题)。**无新增时间/天气/地点字段**(本轮无数据源,勘察已定不做)。**实现(守 ADR-003 边界,纯展示 + 现成 store 字段、零平台 IO、不引动画库)**:`SceneBanner` 组件 + `scene.ts` 两个纯函数——`sceneImageUrl`(已配图世界→`/scenes/<archetype>.webp` / 未配图/未知→`null` 优雅降级不显图)+ `dangerLabel`(low/medium/high/extreme→低/中/高/极危,未知回落原串);全出血负 margin 贴列顶 + 固定高度保缺图不塌;取代旧 `.header` 死样式。**图**:`web/public/scenes/`(Vite publicDir,后端零改),先落 4 张占位 webp 验通「选键→取图→渲染→渐变→降级」链路,后 Felix 给正式锚图就地转换(2752×1536 png → 宽 1200 等比缩、不拉伸变形、q=84,单张 49–88KB);cthulhu/rules_creepy 锚图右下角有水印 → 底部渐变纯 `var(--bg)` 起始 94%→86%(底部 ~14% 实心)盖住,纯 CSS 不动图。**订正 Felix 命名**:brief 说 `rules_creepy.png→rules.webp`,但前端按 archetype id 找 `rules_creepy.webp` → 保 `rules_creepy.webp` 守「代码零改」。**纯前端零后端、无 ADR、`schemaVersion` 不动**。**2 commit**:`c84ac0d`(组件 + 纯函数 + 占位图 + 集成 + CSS)+ `0d30122`(四世界正式锚图替入 + 底部渐变加厚盖水印)。前端 `npm test` **43 绿**(36 + 7:scene 纯函数 4 + SceneBanner 组件 3 含状态栏/选图/缺图降级)+ lint/build 全绿;webp 随 Vite publicDir 进 dist。**Felix 浏览器冒烟验通**:四世界 banner 各显对应真图、渐变自然融白底、水印已盖、正文白底可读、危险度中文、回合数正常。**#8 衍生三条「封面随世界演变」仍在 [打磨与愿景 backlog §7](phase2-polish-and-vision-backlog.md) 冻结**(均依赖主题 A 持久世界记忆,非纯前端范围)。走 b 方案 ff 合并 `main`、**未 push、待 Felix 点头**。
- **完成(#1 选择反馈定性版落地 + Felix 真 key 冒烟验通,2026-07-01)= 打磨期第三刀,v2.1 打磨轮(#3+#8+#1)全部收口**:给决策圈每个选项配一句**定性风险/代价/张力提示**(`availableActions[].hint`),让「选择有重量」。**动手前勘察订正**:`hint` 字段/wire/校验(`GameSchemas` `optionalString` 容忍)/前端 `DecisionCircle` 条件渲染**端到端已存在**、引擎纯 `deepCopy` 透传 → **#1 = 把已有做厚、唯一缺提示词引导**(比 scoping 时估的「选项元数据 schema 扩展」更轻),故**纯提示词 + 前端样式 + 文档,不动 schema/引擎/`schemaVersion`(保 "0.4")**。**决策**:[ADR-011](adr/ADR-011-action-hint-narrative-metadata.md) 采纳=**范围/边界 ADR**(非架构改动),核心边界逐字锁定「**hint = 定性风险/代价元数据、不含精确成功率数字、引擎只读透传永不 gate/掷骰;真概率掷骰 = #1.5 独立未来项,不在本轮**」——守 ADR-008 引擎无知,「让 AI 写 hint、引擎不裁决」同 ADR-010「让 AI 标 outcome、引擎只读」哲学。**4 slice 各 atomic commit**:**Slice 1**(`464b737`)ADR-011 落档 + README + ROADMAP §五 ADR 索引;**Slice 2**(`95344cd`)提示词引导 lockstep 四处——world-gen(`prompts/world-gen.md` v0.5 + `WorldGenPromptBuilder`)把 `hint 可空` 升为「每选项一句定性风险/代价/张力提示、不写精确成功率数字」+ event-loop(`prompts/event-loop.md` v0.9 + `TurnPromptBuilder`,此前完全没提 hint,本版补齐同一条),两 `.md` 各升版号、明写「hint 是叙事提示不据此判定」呼应不掷骰边界;**Slice 3**(`95b6de7`)前端 `DecisionCircle` hint 成选项文字下方独立小字(muted 次级层级、缩进对齐正文起点、缺失不显不塌,守 ADR-003 纯展示);**Slice 4**(`a4aebdb`)#1.5「选项真概率掷骰」入 [打磨与愿景 backlog](phase2-polish-and-vision-backlog.md) 冻结、交叉链 ADR-011。`mvn test` **216 绿**(212 baseline + 4:两 builder hint 指令存在性 + 四处 lockstep 守护 + `toClientState` hint 路径消毒)+ 前端 `npm test` **47 绿**(43 + 4:DecisionCircle 渲染 / hint 显隐 / 点击回传 id)+ lint/build 全绿、**golden parity 字节级零回归**(`EngineGoldenTest` 不变,引擎一行未动)。**Felix 真 key 浏览器冒烟验通**:四世界各起一局,选项定性风险提示稳定出现、贴合选项、无精确百分比、正文未顶爆 A-1、无泄露、样式清爽。**走 b 方案 ff 合并 `main`、待 Felix 点头 push**;CONTEXT/`schemaVersion` 不动(纯提示词/前端/文档)。**至此 v2.1 打磨轮三刀(#3 数值行为化 + #8 静态氛围图 + #1 选择反馈定性版)全部落地 + 冒烟验通,四个世界「有重量」目标达成。**
- **完成(混合模式 round 1 启动·ADR-012 轴合并落档 + 合并函数已实现未接线,2026-07-02)= Phase 3 旗舰模式开工第一刀**:v2.1 打磨轮收口后押旗舰模式(混合模式),round 1 彩蛋=修仙×规则怪谈。**动手前勘察结论**:引擎 / 校验 / DTO 下发 / 前端面板 + `bands` **全 key-agnostic**,天然吃任意轴数 + 多致命轴——**唯一无先例的是两轴撞键**(hp 两边都有,displayName 气血 vs 体力),`ArchetypeRegistry.meta(id)` 现只按单 id 取一块、无合并入口。**[ADR-012](adr/ADR-012-hybrid-axis-merge-strategy.md) 采纳**=host 优先 + 语义换皮、引擎不动:融合轴=按 key 并集,撞键 **host 优先**(host=修仙、修士识海主场 → hp 取气血、foreign 同 key 轴并掉),外来轴带**显示层换皮 override**(规则怪谈 san→道心、bands 修仙口吻重写 清明/动摇/崩缺;**key/axisRole/lethal 不变 → 引擎无感**,守 ADR-008),结果轴集={气血 hp、灵力 mana、境界 realm、道心 san} 4 轴、致命轴={hp,san}。**2 slice 各 atomic commit**:**Slice 1**(`844cac9`)ADR-012 落档 + README + ROADMAP §五 ADR 索引;**Slice 2**(`be0ac7e`)`ArchetypeRegistry.mergeAxes(host,foreign,skins)`(并集 + host 撞键优先 + `AxisSkin` 换皮)+ `cultivationRulesCreepyAxes()` combo + **派生复用单一真理源**(抽 `accumulationKeys`/`nonLethalKeys`/`axisDisplayNames(List<Axis>)` 静态派生、`GameInitService` 三私有派生改委托,行为保持、别新造)+ 6 单测(并集 / 撞键 host 优先取气血 / san 换皮道心且 key·axisRole·lethal 不变 / 角色致命保持 / 融合喂现有派生得 `accumulationKeys={realm}`·`nonLethalKeys={mana}`·致命集={hp,san} / 纯无副作用)。**合并函数是「已实现、已测、暂未接线」的休眠件**(类比 `resolveBand` 先纯函数后接线;让两 archetype 真正走进 init 属融合协议 ADR、本刀不碰 init/world-gen)。**引擎 / 校验 / `schemaVersion`(保 "0.4")一律不动**、golden parity 字节级零回归,`mvn test` **222 绿**(216 baseline + 6)、前端不变。**ADR-012 无需真机冒烟**(纯逻辑、未接线,体感留到融合世界能玩时一起验)。走 feature 分支 `phase3/hybrid-axis-merge`,**待 Felix 批准再合并 `main`/push**(未自动合并)。场景=③识海遗蜕待融合协议 ADR。
- **下周计划**:混合模式 round 1 继续——起**融合协议 ADR**(把两 archetype 真正接进 init:hybrid 入参 / world-gen fusion meta-prompt 调和 ruleForm+worldview+rulesCarryTruth / 用 `mergeAxes` 接线播种;ADR-012 休眠合并函数届时接活),端到端跑通后再回写 CONTEXT §一 fusion / §三 的 host 优先+换皮约定(避免此刻写空头约定)。**融合协议 ADR 是独立工作单元,Felix 新窗口起**。并行待办照旧:Phase 2 后续主线(分享长图 / 账号 + 云存档 / 内容安全审核网关 / 成本控制)、末日「饥饿」改名独立小单元、视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝。
