# Week 2 复盘(2026-06-22 ~ 2026-06-28)

> 从 `docs/ROADMAP.md` 周度日志归档(「最近三周,循环覆盖」滚动,2026-07-14 移出)。原文逐字保留。

- **决策**:ADR-003(前端栈与 Taro 边界)已采纳——React+Vite H5 先行,**用接口纪律(非实现)占住小程序迁移边界**:逻辑/状态/类型/展示层平台无关(Taro 直接复用、禁引用平台 IO),网络/流 IO 全收进 `web/src/api/` 薄适配层(镜像后端 `LlmClient`/`TokenStream` 接缝哲学,ADR-001/005);状态 Zustand、样式原生 CSS/CSS Modules(Taro 迁移税最低);Phase 1 **不写任何小程序代码**,只保证边界接缝在对的位置(WS 预案记录不实现)。落档单独 commit(`4eceec3`),与实现分开。
- **完成(前端 H5 整局闭环)**:按 ADR-003 由内到外落 `web/` 前端整局闭环(init→回合→结局)。**①`api/` 适配层**(平台 IO 唯一集中地):`contract.ts` 暴露 provider-agnostic 契约(`GameApi` + `TurnStream{onNarrative/onDelta/onEnding/onError/onClose}` + `GameApiError`);`sse.ts` 用 `fetch`+`ReadableStream` 流式解析 SSE(跨 chunk 帧边界扫描);`h5GameApi.ts` 接已实测定型的 wire(init plain POST ADR-007 / 回合 SSE 命名事件 ADR-006),映射四类语义事件。**②状态层** `state/gameStore.ts`(Zustand):消毒 client world + 流式增量(narrative 累加 / delta 落数值&规则面板 / ending 出画面 / 可恢复 error 回 awaiting+提示);`createGameStore(api)` 工厂可注入 mock,测试零网络。**③回合循环 UI** `features/game/`:散文区(开场 client-side 逐字 reveal `useTypewriter`,回合实时流直渲)+ 决策圈(只选 id,不开自由文本)+ hp/san 数值面板 + 规则面板(已验证高亮,**绝不渲染 `isTrue`/`hiddenLogic`**)+ 整局流程容器(start→loading→reveal→回合→结局 / world-gen ERROR 出「重新生成」)。**④边界硬线**:`eslint.config.js` `no-restricted-globals` 禁逻辑/状态/展示层引用 `fetch`/`EventSource`/`WebSocket`/`wx.*`(`api/` 与测试豁免);自查 `state/`/`features/` 零平台 IO。**测试 14 绿**:api 层(mock fetch + 合成 SSE 字节流跨 chunk 切分 + 四类事件分发 + init 解析 + 502/网络/404 失败归一)、状态层(合成事件序列推进,含 ending/error/守卫分支);lint/build 全绿。工程:zustand + vitest/jsdom/testing-library,vite dev proxy `/api`→`:8080`,退役脚手架占位(`sampleWorld`/`App.css`/`.gitkeep`)。commit `78bd5d6` @ `phase1/frontend-h5`。
- **偏差如实记**:brief 假设「H5 实现用原生 `EventSource`」,但回合端点是 `POST /api/game/{saveId}/turn` 带 JSON body,而浏览器 `EventSource` 只能 GET、不能带 body —— 接不了这个 wire。改用 `fetch`+流式读 body 解析 SSE(POST-SSE 标准做法)。**ADR-003 边界不受影响**:逻辑层依旧只见 `TurnStream`、永不见传输细节,反而更纯(连 `EventSource` 都不用)。已在 `api/sse.ts` 顶注与 ADR/CONTEXT 记明。
- **约定回写**:CONTEXT v0.5(§三.13 前端跨端边界:平台无关层禁平台 IO + `api/` 薄适配层 + provider-agnostic `TurnStream` 流接口 + eslint 硬线);JSON `schemaVersion` 仍 "0.2"(字段未变)。ADR 索引加 ADR-003、移出待决策(随 ADR 落档 commit)。
- **完成(真 key e2e 首次通关 + 前端批合并 main)**:`DEEPSEEK_API_KEY` 就位,Felix 以**玩家视角**真 key 从 init 玩通一局规则怪谈到结局——前后端整局打通的首次真实验,**Phase 1 里程碑「朋友能用手机从头玩到一个完整结局」技术闭环达成**(本窗只架环境 + init 探针,回合流式手感由 Felix 本人在浏览器判,非脚本观测)。验过后解封合并:`mvn test` 139 绿 + 前端 `npm run test`(14)/`lint`/`build` 全绿 → 前端批 **ff 合并 `main`** 并 push origin(`4eceec3`/`78bd5d6`/`0b1ebeb`/`02f2faf`);删旧分支 `phase1/frontend-h5` + `phase1/event-loop-core`(后者提交早已全含 `main`)。**ADR-003 §3 预案主次序校正**(随上轮 commit `02f2faf`):H5 实测确认回合走 `fetch`+`POST` body+流式读 chunk(非原生 `EventSource`)→ Phase 4 小程序 `wx.request`+`enableChunked` 分块回调(与 H5 同构、复用最多)升**主预案**、WebSocket 降**备选**;CONTEXT v0.6 同步(纯文档,`schemaVersion` 仍 "0.2")。
- **决策(可玩性 backlog + A 计划限范围)**:e2e 暴露反馈按 Phase 分类入档(防范围膨胀,见 `docs/phase1-playability-backlog-and-polish.md`)——**现在做**仅两条:**A-1 叙事长度约束**(提示词层零代码,所有世界共用的 event-loop 体验)+ **A-2 规则高亮**(前端把后端已产的 `delta.discoveredRules` 点亮,非新功能);**推迟**:配图管线(②,Phase 2 后,涉图像生成 + 成本/延迟 + 图过审)、难度系统(③b,Phase 2/3)、多世界 archetype 选择(④,Phase 2 主线,UG Engine 已留好)。
- **下周计划(已收口,见下条)**:从干净 `main` 起 `phase1/polish` 做 A-1/A-2 两批(互不依赖、各自单独 commit),做完架 e2e 复玩环境由 Felix 本人验体感(A-1 读起来累不累 / A-2 触发规则时面板高亮明不明显);**不替 push/合并**,等 e2e 复玩定后再 ff;视觉打磨(`frontend-design` skill 定方向);Phase 1 排期纳入 ICP 备案 + CloudBase 部署接缝。
- **完成(A 计划打磨 + Phase 1 收口)**:从干净 `main` 起 `phase1/polish` 落 A 计划两批(各自独立 commit)。**A-1 叙事长度约束**(`b5d42c7`,提示词层零代码):给 event-loop 每回合散文加长度约束(目标 2–4 句/约 120–200 字、硬上限 ~280 字,短叙事主体 + 可选细节,**明确不为压长度牺牲氛围/逻辑**),lockstep 改人类可读资产 `prompts/event-loop.md` v0.4 + 运行时真正发模型的同义副本 `TurnPromptBuilder.SYSTEM`(沿用上次清洁度修法:只改 `.md` 运行时失效)。**A-2 规则高亮**(`e2c3bde`,纯前端展示层):**查清工作量后确认是「点亮已有数据」非新功能**——后端早产 `delta.discoveredRules`、store 已落 `discoveredRuleIds`、`RulesPanel` 已据此渲染,问题是旧高亮太弱;强化为已验证规则左侧琥珀警示条 + 底色 + accent 「已验证」徽标(持久)+ 本回合新坐实加一次性 `ruleFlash` 脉冲(把视线从剧情拉到规则,降「没头绪」),新增 `--accent`/`--accent-soft` 主题色(深浅各一档),守 ADR-003 边界(无平台 IO、逻辑/状态层零改、永不渲染 `isTrue`/`hiddenLogic`)。两批验:`mvn test` 139 绿 + 前端 `npm run test`(14)/`lint`/`build` 全绿。**Felix 真 key e2e 复玩验通(叙事长度合适、规则高亮帮到「有头绪」、顺畅玩通一整局)→ ff 合并 `main` 并 push(`b5d42c7`/`e2c3bde`),删 `phase1/polish`。Phase 1 里程碑「朋友能用手机从头玩到一个完整结局」正式达成,进度表 Phase 1 行 🟨 → ✅(2026-06-23)。** ADR/CONTEXT/`schemaVersion` 均不动(纯进度收口 + 提示词资产 + 前端展示层)。
- **下周计划(已收口,见下条)**:进 **Phase 2 · 多模式**——先验「生成管线可扩展性」(UG Engine 复用、`archetype` 入参开放),挑数值结构最接近规则怪谈(hp/san)的模式如 **末日生存**先行,逐个独立验新模式 world-gen(world-gen 是最高风险生成步,每个新模式独立验、不外推);**多模式设计仍在 Project 窗口讨论,定稿后再单开 Code 窗口起 Phase 2 第一批**。并行待办:视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝。
- **完成(Phase 2 第一批·末日生存,行 ⬜ → 🟨)**:末日生存(`apocalypse`)模式落地 = **验多模式管线能跑通、给后续所有模式立模板**,真 key 冒烟验通。**决策**:[ADR-008](adr/ADR-008-multi-mode-extension-architecture.md) 采纳(C 偏 A:引擎/校验对数值 key 语义无知 + per-archetype 轻量元数据非强制校验;衰减 AI 落引擎无知;world-gen 通用骨架+注入块;init archetype 入参;逐模式独立 world-gen 冒烟硬验收门)。**事实订正(动手前摸代码发现,停下报 Felix 拍板)**:brief 决策 1「引擎一行不改」基于「引擎已 key-agnostic」的错误事实假设——实际核心 key-fixed 到 `{hp,san}`(`validateWorld`/`validateTurn` 各 require san、`Engine` 具名 hp/san 字段 + snapshot 重建 `{hp,san}`、`buildDelta` 硬编码);通吃 `{hp,hunger}` 必须把引擎/校验器/buildDelta **一次性机械泛化为 key-agnostic**(遍历 `attributes` map,严格限「字段名→字典遍历」、结算/clamp/跳变/兜底逻辑一行不动),由 **golden parity 139 逐字节守 `{hp,san}` 零回归**(不绿即停)。**6 commit**(由纯到脏):ADR 落档(`b4b598d`,实现前独立,含 README/ROADMAP §五 ADR 索引 + 设计稿入 docs/ + CONTEXT **v0.7**:§三.5 展开 attributes 开放字典/引擎数值无知/衰减 AI 落 + §三.14 per-archetype 元数据结构,`schemaVersion` 仍 "0.2")→ 核心 key-agnostic 泛化(`ee0435d`)→ per-archetype 元数据 `ArchetypeRegistry`(`b35d023`,末日 hp/hunger=体力/饥饿 + 规则怪谈 hp/san=体力/理智,两模式同元数据驱动路径)→ world-gen + event-loop 提示词通用骨架+注入块(`7a126d3`,lockstep `.md` + 运行时副本,末日 hunger 衰减注入,保叙事清洁度/长度硬约束)→ init archetype 入参真正接受+枚举校验(非法/未开放→400)+ 数值轴元数据下发(`5be122c`)→ 前端数值面板按元数据动态渲染(`f53c14a`,体力/饥饿 vs 体力/理智,守 ADR-003 边界)。`mvn test` **154 绿**(139 baseline 字节不变 + 15 新)、前端 `npm test` **18 绿** + lint/build 全绿。**真 key 冒烟验通(Felix 读数拍板,四轴齐过)**:world-gen 首次有效率 **3/3 零修复**、格式完整 hunger 轴齐、**AI 落 hunger 衰减稳定**(40→35→…→0 每回合 -5、针对性只动衰减轴、推到 turn 15 未忘、触底触发 `starved_to_death` 端到端)、**叙事无泄露**、末日整局闭环(init→回合→结局)真 key 首次贯通、生存压力体感真实 → **ADR-008 决策 1(key-agnostic 泛化)+ 决策 2(AI 落衰减)实测成立**。6 commit ff 合并 `main`、删 `phase2/apocalypse-mode`。**偏差/待办如实记**:前端 archetype 选择 UI 按 ADR-008 决策 4 推迟(末日经 init 入参 + curl 探针验,前端默认仍 rules_creepy)→ 独立小批;`/tmp` 末日真实 raw + SSE 转录接进测试夹具替换合成数据(独立小活,见 FINDINGS,非本批)。
- **完成(A 计划·archetype 选择 UI + 每世界专属卡片氛围,2026-06-25)**:补 ADR-008 决策 4 推迟的前端选择 UI(已验证闭环的低风险 UI 改造小批)。**功能**:玩家进游戏见「选择你的世界」第一屏 → 选模式 → init 该 archetype → 进 Phase 1 已验证整局闭环(闭环零改动);末日不再只能 curl 驱动。**后端(小改)**:轻量端点 `GET /api/archetypes`(`ArchetypeController` 薄适配 + registry `listForSelection()`:已激活 rules_creepy/apocalypse 在前 + 已知未开放 life_sim/cultivation/cyberpunk 占位灰显「敬请期待」),`ArchetypeMeta` 加玩家可见文案字段 `tagline`/`vibeTag`(**仅选择屏展示,不进 world-gen 注入**,守 CONTEXT §三.3)。**前端(主体,守 ADR-003)**:`api/listArchetypes`(平台 IO 只在此层)+ store `loadArchetypes`/`lastArchetype`(网络走 `api/` 接缝、目录在 INITIAL 之外维护 reset 不清)+ `ArchetypeSelect` 组件,删 `GameScreen` 硬编码 `rules_creepy`。**e2e 反馈驱动一轮活体感迭代**:静态卡 → 统一柔光(太弱)→ 拍板**每世界一套 CSS 专属氛围**(卡片即世界预告片、静止可见):规则怪谈冷蓝灰 + 暗角 + 偶发冷光闪烁(灯将灭)+ hover 红渗(危险);末日锈土 + 颗粒纹理(SVG feTurbulence)+ 沙尘横飘 + hover 起风;灰显卡死寂反衬。可扩展(加世界=配一套 `.cardXxx` 主题 + `vibeClass()` 分支)、`prefers-reduced-motion` 关运动、纯 CSS 不引 GSAP(对 Taro/小程序支持差、违 ADR-003,记 backlog)。**3 commit**:选择 UI(`8e15ce4`)+ InitRequest 陈旧注释订正(`f8ec6df`)+ 每世界专属卡片氛围(`0993c0d`)。`mvn test` **156 绿**(154 + registry `listForSelection` + `ArchetypeController`)、前端 `npm test` **27 绿**(18 + listArchetypes api 3 + loadArchetypes/lastArchetype store 4 + ArchetypeCard 组件 2)+ lint/build 全绿;Felix 浏览器真 key e2e 验通(选哪个进哪个 + 选择屏体感 + 每世界氛围有吸引力)→ ff 合并 `main`、删 `phase2/archetype-select-ui`。CONTEXT v0.8(§三.15 `GET /api/archetypes` 契约 + registry 玩家文案字段,`schemaVersion` 仍 "0.2")。**世界库 backlog 入档**([14 世界愿景分级地图](world-library-expansion-backlog.md),持续加世界引擎的分级地图,克苏鲁批引用)。
- **下周计划(已收口,见下条)**:进**加世界流水线起步,下一个=克苏鲁**(`cthulhu`/规则怪谈近亲、最便宜,练手再证「加世界=一条元数据 + 一个 world-gen 注入块 + 一次独立真 key world-gen 冒烟」的流水线;world-gen 最高风险生成步,规则怪谈/末日成功不外推)——**克苏鲁是独立工作单元**:设计在 Project 窗口起、实现新 Code 窗口起(本窗不起)。并行待办照旧:视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝、Phase 2 后续主线(分享长图 / 账号云存档 / 成本控制)。
- **完成(Phase 2·克苏鲁,加世界流水线第一次复用成功,2026-06-25)**:克苏鲁(`cthulhu`)落地 + 真 key 冒烟验通 = 末日趟出的加世界流水线第一次正式复用,**理想形态达成:又短又顺、不碰引擎、不出新 ADR**(本批本身是「流水线成色」测试)。**核心定位**=禁忌知识的代价(非换皮规则怪谈):签名轴 `hp`/`san`(复用)+ `knowledge`(禁忌知识,克苏鲁特有**累积型双刃**——求知则涨=力量,knowledge 越高 san 流失越快=代价)。**实现(commit `3227a31`)**:registry 加克苏鲁条目(KNOWN 枚举 + 激活,选择屏自动出第三张卡、前端零改)；world-gen + event-loop 通用骨架不动 + 克苏鲁注入块(世界观/禁忌知识渐揭 ruleForm/`knowledge` 累积 + **`knowledge↔san` 联动**:AI 落、引擎无知,守 ADR-008 决策 1/2);`AttributeAxis.decay` 泛化为 `behaviorHint`(涵盖衰减/累积/联动、引擎仍不读)+ 新增 `accumulating()` 工厂;提示词 lockstep(`prompts/world-gen.md` + `event-loop.md` v0.6 + 运行时副本);前端卡片专属氛围(幽绿深渊 + 触手般蠕动暗影,纯 CSS、`prefers-reduced-motion` 关运动)+ `StatsPanel` 通吃三轴(体力/理智/禁忌知识)+ `Archetype` 类型加 cthulhu。`mvn test` **162 绿**(156 baseline 字节不变含 golden parity 零回归 + 6 克苏鲁)、前端 `npm test` **29 绿**(27 + StatsPanel 三轴 + 克苏鲁卡片)+ lint/build 全绿。**真 key 冒烟 Felix 浏览器验通**:玩通一局到 knowledge 51 / san 0 → 理智崩解疯狂结局,**knowledge↔san 联动成立**(「越求知越疯」张力真实)、F-012 提示词兜住(knowledge 全程正基线、没被打到 0)、克苏鲁题材独特(禁阅区/禁忌典籍/疯人院)、规则高亮 + 整局闭环在新世界复用成功 → ff 合并 `main`、删 `phase2/cthulhu-mode`。**架构信号如实记(两条同类,FINDINGS F-012/F-013)**:均「骨架/引擎某假设对新世界类型不成立」——F-012 引擎「≤0=触底死亡」只对 depletion 轴成立、对 accumulation 轴(knowledge)错;F-013 骨架强制 `rules[].isTrue` 与「非真假守则」型 ruleForm 打架(首发首过修复后过、玩家无感)。**本批均提示词兜（不碰引擎、不改提示词框架),引擎正解留修仙批一起做 + 出 ADR**(届时有两个累积轴样本 + 两类规则形态,设计更稳)。CONTEXT **v0.9**;设计稿入 `docs/phase2-cthulhu-mode.md`。
- **下周计划**:进 **修仙**(世界库第二级,`cultivation`/灵根/境界全新数值体系=模板真正的压力测试 + 中文用户最想要之一)——**且要正面解决 F-012/F-013 两个架构缺口**:F-012 累积轴角色(depletion vs accumulation,触底判定只对 depletion 轴 + accumulation 轴另设死亡条件)、F-013 骨架 isTrue 对非真假规则世界可选/按 ruleForm 分派,两者一起做引擎正解 + 出 ADR(修仙有 knowledge + 修为/境界两个累积轴样本,设计更稳）。**修仙是独立工作单元**:设计在 Project 窗口好好起、实现新 Code 窗口起(本窗不起)。并行待办照旧:视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝、Phase 2 后续主线(分享长图 / 账号云存档 / 成本控制)。
- **完成(Phase 2·修仙 + ADR-009 架构正解,2026-06-26)**:修仙(`cultivation`)落地 + 真 key 冒烟**验通架构** = Phase 2 至今最重一批(**重心在架构正解、不在修仙深度**)。**ADR-009 采纳**(`843b642`,实现前独立落档 + 修仙设计稿入 docs/ + README/ROADMAP §五 ADR 索引)。**切分由架构到内容、parity 优先**(异于前几批由纯到脏,因引擎正解有回归风险、先在 parity 网下做):**①F-012 引擎正解**(`c8191db`)——`AttributeAxis` 加 `axisRole`(DEPLETION/ACCUMULATION)+ `depleting()` 工厂;`Engine` 加 3 参构造 `(world,mapper,accumulationKeys)`,触底 `anyAttributeBottomedOut`/兜底 `forceBottomOutEnding` **按角色 gate**(accumulation 轴 ≤0 不致死);播种层 `GameInitService`→`GameSessionManager.create` 据 per-archetype 元数据传累积轴 key 集合,**引擎仍不懂具体轴语义、只据集合 gate**(守 ADR-008);2 参构造默认空集=全 depletion → **golden parity 字节级守零回归**(`EngineGoldenTest` 不变),克苏鲁 knowledge「提示词正基线兜」升级为引擎根治。**②F-013 正解**(`ba8c56c`)——`validateWorld` 的 `isTrue` 必需→可选(`optionalBoolean`,校验零分派)+ `schemaVersion` "0.2"→"0.3"(`WORLD_SCHEMA` **接受双版本**:golden/validator-parity/init 夹具是录制的真实 "0.2" 产出,硬翻只认 0.3 会让 parity 集体失效 → 接受双版本守护城河 + 向后兼容,"0.1" 仍拒;**此判断记在 ADR-009 已知代价 4**),前端 `SCHEMA_VERSION`/`Rule.isTrue` 同步。**③修仙内容**(`fc9fb7d`)——registry cultivation 条目激活(三轴 hp/mana/realm=气血/灵力/境界,境界=第二个 accumulation 样本+主角轴、灵力 depletion 资源池、灵根做 traits;`ArchetypeMeta` 加 `rulesCarryTruth`=F-013 注入分派,修仙心法守则不带 isTrue)+ world-gen/event-loop 通用骨架据 `rulesCarryTruth` 注入 rules 措辞 + 修仙注入块(lockstep `.md` v0.7、world-gen 产出升 0.3)+ 修仙卡片 CSS + `StatsPanel`/`vibeClass` 通吃 mana/realm。`mvn test` **173 绿**(162 baseline + 5 F-012 + 4 F-013 + 1 registry + 1 prompt)、前端 `npm test` **30 绿**(29 + 修仙 StatsPanel)+ lint/build 全绿。**真 key 冒烟 Felix 浏览器两局验通架构**:境界 accumulation 累积**不误死**(**F-012 成立**)、rules 无 isTrue **无首过修复**(**F-013 成立**)、引擎 parity 零回归。CONTEXT **v1.0**(§二 isTrue 可选 + schemaVersion 0.3、§三.8 触底按 axisRole、§三.14 axisRole+rulesCarryTruth、§三.4/§三.15 cultivation 激活)、FINDINGS **F-012/F-013 标「ADR-009 根治、已关闭」**。
- **完成(UI 配色微调,纯 CSS,本批一并)**:Felix e2e 反馈两处——(a) 规则怪谈卡片冷蓝偏「致命感不足」→ 加**血色**(底部渗血 radial + 血色左缘 + 既有 hover 红更浓),强化「读错付出代价」;(b) 修仙卡片原青绿**撞克苏鲁深渊感**→ 改**白色仙气**(近白底 + 飘动云彩 `cloudDrift` + 微金光点 `spiritRise`,深色文字保可读,亮底卡 tag/钩子改深金墨色)。沿用 `vibeClass()` 可扩展形态、纯 CSS、`prefers-reduced-motion` 关运动。
- **架构信号如实记(冒烟暴露,FINDINGS F-014,先诊断未改)**:**系统性结局判定 bug**——修仙气血 8/灵力 0 濒死却给「守园有功/大比夺魁」光明结局(**两局复现**)。诊断根因:**主因**=AI 提议不匹配的成功结局,`Engine.apply` §4.4 **只 gate `id` 存在性、不 gate 死活/数值状态** → 接受、`aiAccepted=true` → §5 兜底被跳过;提示词侧 world-gen/event-loop **均无「结局须匹配核心数值死活」约束**。**次因**=§5 `forceBottomOutEnding` 用**英文轴 key vs 中文 condition** 不匹配 → 回落 `firstEndingId`(常是好结局),所有模式都在、只是单轴模式 AI 稳定给死亡结局(F-010)故极少点火被掩盖。**多轴露馅**:境界(accumulation)「成功」信号与 hp/灵力「濒死」冲突致 AI 往圆满解读。修法方向:**(A)** 提示词强化「结局须匹配死活」(便宜·不动核心·无 ADR·靠 AI 自律)/ **(B)** 引擎结局极性 gate(健壮·需 endings 加极性元数据 + 动 event-loop 核心 + ADR)。
- **完成(F-014 部分缓解 A + §5 + 修仙批合并 main,2026-06-26)= 走路 A**:Felix 拍板修仙批先合并、F-014 根治另起独立 B 批。**A 提示词强化【已做】**(commit `52b0cbe`,lockstep `.md` + 运行时副本):event-loop(`TurnPromptBuilder`)+ world-gen(`WorldGenPromptBuilder`)结局生成加硬约束——核心 depletion 数值濒零(≤约 10)/ 叙事濒死时只能给失败结局、绝不给成功结局,宁可 null;world-gen 侧并要求 condition 绑定死活前提 + 点名数值轴中文名。**§5 兜底确定性 bug 顺手修【已做】**:`findEndingByConditionMentioning` 英文 key vs 中文 condition 永不命中 → 回落 `endings[0]` 好结局,改为**优先按轴中文名匹配**(`Engine` 加 4 参构造收 `axisDisplayNames`,播种层据元数据传入),中文名缺失才回落 key;**golden parity 字节级零回归**(2/3 参构造无名 → 回落 key=旧行为)+ 2 新单测。`mvn test` **175 绿**(173 + 2 §5)+ 前端 30 绿。**但 A 仅软引导、未根治**:冒烟实测**连三局濒死仍得成功结局**(A 不拦主路径 §4.4;§5 修复只在 AI 给 null 兜底时生效)→ **F-014 标「缓解中,B 根治」、不关闭**(对照 F-012/F-013 真验通已关闭);根治由 **B 批(引擎结局极性 gate,ADR-010)** 承担。**修仙批(含 A+§5)8 commit ff 合并 `main` + push origin、删 `phase2/cultivation-mode`**;CONTEXT v1.0 / FINDINGS F-014 措辞订正(部分缓解 ≠ 根治)/ F-015(灵力轴角色粒度存疑,独立待办)随批。
- **下周计划**:起 **B 批 = ADR-010(引擎结局极性 gate 根治 F-014)**——给 `endings[]` 加极性(success/failure)元数据,引擎 §4.4 拒绝「致命轴触底/濒死时的成功结局」、§5 据极性确定性挑失败结局(动 event-loop 核心 + schema/registry → 出 ADR-010);**独立工作单元,Felix 新窗口起**。并行待办照旧:视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝、Phase 2 后续主线(分享长图 / 账号云存档 / 成本控制)、F-015(灵力轴角色粒度)。
- **完成(结局极性 gate · ADR-010 根治 F-014 + 关闭 F-015,2026-06-26)= B 批,真 key 冒烟两局验通根治**:F-014(濒死得光明结局)A 提示词软引导连三局压不住——**AI 讲故事惯性盖过提示词约束**,软引导压不住 → 引擎硬把关根治。**ADR-010 采纳**(`1369de2`,实现前独立落档 + 设计稿入 docs/ + README/ROADMAP §五 索引;**第二次正面动 `Engine.apply` 结局判定**,继 ADR-009 触底按 axisRole 之后)。**切分由架构到内容、parity 优先**:**①schema**(`ec3c3f8`)`endings[].outcome`(可选极性 success/failure/neutral,**AI 标、引擎只读**;`toClientState` 不剥它=结局元信息非作者隐藏字段)+ `AttributeAxis.lethal` + `schemaVersion` "0.3"→"0.4"(`WORLD_SCHEMA` 接受 {0.2,0.3,0.4} 守 golden/validator-parity/init 夹具),前端 `SCHEMA_VERSION`/`Ending.outcome` 同步;**②§4.4 引擎 gate**(`08ff650`)`Engine` 加非致命 depletion 集构造 + `ENDING_GATE_THRESHOLD`(10),步骤 9:致命轴濒零(≤10)且 AI `outcome==success` → **引擎拒绝该成功结局、`pickFailureEnding` 据极性 + 致命轴中文名确定性挑 failure**;§5 兜底/§10 触底据极性挑失败 + 触底收窄到 lethal 轴 → **golden parity 字节级守零回归**(默认空非致命集=全 depletion 致命=现状,`EngineGoldenTest` 不变);**③lethal 元数据**(`2dee42a`)hp/san/hunger/气血=true、**灵力 `AttributeAxis.resource()` lethal=false**(枯竭=力竭非必死,关闭 F-015),播种层 `GameInitService`→`GameSessionManager` 传非致命 depletion 轴 key 集合 `{mana}`;**④world-gen 注入**(`73e4b68`)AI 标 outcome 极性指引(失败/陨落/死亡=failure,圆满/突破/逃生=success),lockstep `prompts/world-gen.md` + 运行时副本 `WorldGenPromptBuilder`,A 濒死约束保留作软第一层(双保险:A 减否决频率 + B 硬兜底保正确)。引擎**只读 AI 标的 outcome + lethal 轴值,不懂结局语义**(守 ADR-008,「让 AI 标 outcome」是守无知的关键)。`mvn test` **188 绿**(175 baseline + 13)、前端 **30 绿** + lint/build 全绿。**真 key 冒烟 Felix 浏览器两局验通根治**:① 气血 0(真死)→ gate 拦下成功结局、给「身死道消」失败结局 ✅;② 气血 5(濒死未死)→ gate **不矫枉过正**、不强制结束,给四个继续行动选项(挣扎爬起/吞辟谷丹/威吓拖延/闭目等死)——**该死时给失败结局、没死透时让继续,两种边界都对**(数值 5/0/38 + 叙事「虎口崩裂、妖兽再压近」+ 选项三者完全对齐,沉浸感满);灵力两局都 0 但**未误触发死亡/失败**(F-015 lethal=false 成立)、AI 标极性可靠(「标错极性」哨兵未触发)。**这比单纯「濒死给坏结局」更完整——证明极性 gate 既拦爽文、又不误杀濒死求生空间**。CONTEXT **v1.1**、FINDINGS **F-014 标「A+§5+B(ADR-010)根治、已关闭」/ F-015 标「ADR-010 关闭」**。**5 commit ff 合并 `main` + push origin、删 `phase2/ending-polarity-gate`**。
- **下周计划**:Phase 2 至此=五模式可玩(规则怪谈/末日/克苏鲁/修仙)+ 多模式管线/加世界流水线/数值轴架构(axisRole/lethal/结局极性 gate)三层架构正解全部落地验通。**下一步在 Project 窗口定**——候选:Phase 2 后续主线(精彩世界线/奇葩结局一键长图分享、账号 + 云存档、内容安全审核网关、成本控制)、视觉打磨(`frontend-design` skill)、ICP 备案 + CloudBase 部署接缝、或继续加世界。本窗口不起下一批。
