# AI Universe Simulator

基于 LLM 的生成式文字模拟游戏：玩家作为绝对变量介入 AI 动态生成、逻辑自洽的世界。
杀手锏：概念融合·混合模式（世界观杂交）。当前 **Phase 3 收尾转前端视觉轮**
（上线阶段 ③ 部署 + ④ 成本闸门已完成、① 备案冻结、② ADR-004 待启；已上公网
https://wanjie-ai.fly.dev ）。目标用户原定国内微信生态，现走**路线 B**（境外托管、
不备案、暂不进微信生态）先做软启动验证，境内合规整体冻结（见 ADR-015 / 上线全图）。

**视觉移植进度**（ADR-018 四刀切分）：刀 0 severity 契约 ✅ / 刀 1 共享基建 + 规则怪谈 ✅ /
刀 2 修仙 + 钟鸣 ✅ / 刀 2.5 灵脉打磨 + 境界印 ✅ ／ **刀 3 末日、刀 4 克苏鲁待起**。
**最新进度以 `docs/ROADMAP.md` 为准**（本行只为让 agent 一眼知道自己站在哪儿）。

## 协作分工
架构/决策讨论在 **Project 对话**；执行分**两段**——视觉探索走 **Cowork 独立样板间**
（不直接改 `web/src`），写码、测试、提交走 **Claude Code / Codex**（feature 分支 + 测试 +
Felix 冒烟，见 ADR-017 §8）。
**改动请先讲清思路再动手，避免无说明的大改**（展开见下 § Workflow Discipline §1）。

## 动任何模块前先读
@docs/CONTEXT.md   # 术语 / 统一 JSON Schema / 命名与工程约定（约定的真理之源）
@docs/ROADMAP.md   # 路线图、当前进度、待决 ADR

**动手的规矩看本文件下半部分**：§ Workflow Discipline（怎么干活）+ § Motion Constraints（前端视觉/动效）。
详细 ADR / FINDINGS 按需读，不必每次全载，以省上下文。

## 各类产出物落在哪（**只记位置，规矩见 § Workflow Discipline**）
- 技术决策 → `docs/adr/`（Claude Code 可用 `/adr-author`；其它 agent 手写同结构）。
- 进度 → `docs/ROADMAP.md`（Claude Code 可用 `/roadmap-update`）。
- 验证中的发现 → `bakeoff/FINDINGS.md`；约定 / schema 变更 → `docs/CONTEXT.md` 升版本号。
- 提示词是核心资产 → 仓库根 `prompts/`，按管线步骤组织（world-gen / event-loop / …）。
- 运行模型（给玩家生成）= **DeepSeek**（见 ADR-001）；写码用的 agent 模型与它是两回事，别混。

## 仓库结构速查
- `docs/` — ROADMAP / CONTEXT / adr/ + 各批设计稿与 backlog
- `prompts/` — 管线各步提示词
- `bakeoff/` — provider 验证脚手架（client / providers / schema / scenarios / report / run + FINDINGS）
- `server/` — Spring Boot 后端；`web/` — React + Vite 前端
- `.agents/skills/` — 项目级 skill（GSAP 四件装 + adr-author / roadmap-update）

---

# Workflow Discipline（工作流纪律 · 所有 agent 适用）

> **本节是本项目工作流的真理之源**（Claude Code / Codex / 其它 agent 一体适用）。`CLAUDE.md` 只放指针，不再维护第二份全文——两份全文已经漂移过一次（2026-07-23 那批只订正了 `AGENTS.md`，`CLAUDE.md` 直到 2026-07-26 仍写着「Phase 0 / 微信生态」），那次漂移就是本节存在的理由。
>
> 下面每一条都是**曾经付过学费**的，不是通用最佳实践的复述。

## 0. 一句话总纲

**先取证，再动手；动完如实报。** 这个项目里，「看起来对」和「验过是对的」之间的距离，就是所有返工的来源。

## 1. 勘察先行

- **动手前先读现有代码**，读完**先汇报勘察结论**再动笔——尤其是「这个字段/接缝/组件到底存不存在」这类问题。
- **发现与前提不符的事实：停下报告，不自行调和。** 任务 brief 可能基于过时或错误的假设；agent 擅自「把 brief 圆回来」会把一个五分钟的澄清变成一批要返工的代码。
  - 实例：ADR-018 刀 0 的 brief 派生规则与验收标准自相矛盾（禁忌知识是 accumulation 但 `lethal=false`），停下报告后 Felix 裁定新增 `perilAtHigh`；若当时自行调和，会为染色去改引擎会读的 `lethal` 字段。
  - 实例：ADR-015 勘察发现 `Engine` 根本无法从落盘数据回载，与 brief 的「引擎零动」前提冲突，停下报告后改口径为「现有行为零动 + 纯增量恢复入口」。
- **勘察结论要写进交付说明**（哪怕它推翻了 brief 的一句话）——「事实订正」是本项目文档里的常规栏目，不是认错。

## 2. 决策先落 ADR，再实现

- **重要架构决策先落 `docs/adr/`，再写实现**。判据：这个决定会不会**约束以后的批次**（新增跨模块字段、改变某层的职责边界、放开一条既有禁令）——会，就先写 ADR。
- ADR 落档与实现**分开 commit**，ADR 在前（便于「决策独立于实现被审查」）。
- 纯范围/边界性质的决定也值得一条短 ADR（ADR-011 就是把「hint 是叙事元数据、引擎永不据它裁决」这一句钉死）。
- 与之配套：**约定或 schema 变更走 `docs/CONTEXT.md` 升版本号**，不靠提示词反复叮嘱；`schemaVersion` 是否变动**必须在交付说明里明说**。

## 3. 分支、提交与合并

- 从 `main` 切 **feature 分支**干活（`phase4/xxx`、`docs/xxx`、`obs/xxx` 一类）。
- **atomic commit**：一个 commit 一件事，且**每个 commit 单独可编译可测**。切分优先按「架构 → 内容 → 测试」而不是按文件。
- **commit message 写「为什么」**，不只写「做了什么」；被推翻的判断、被裁定的口径、如实记的偏差都写进去——这个仓库的 commit history 是被当档案读的。
- **ff 合并前等 Felix 点头；不擅自 push。** 即使全绿也不代表可合并——冒烟与体感判断权在 Felix。
- 已 ff 合并 `main` 且 push 后即可删分支（不必再问）。

## 4. 如实报，别反应式修补

- **测试 / 冒烟结果一是一二是二**：不粉饰、**不为了收口说通过**。没验的就写「没验」，验不了的写清**为什么验不了**。
  - 实例：刀 2 的钟鸣停顿间隔本地测不了（页面 hidden 时 rAF 冻结、`setTimeout` 被钳到 ≥1s，实测 1232ms ≠ 550ms）——就这么写，并挂进真机回归，而不是含糊说「时序正确」。
- **别反应式修补**：报错/现象异常时，先**取证定位真因**再改。刀 1「灯闪到底有没有发生」凭感觉调了强度是弯路，接上时序探针后一次二分就抓到两个真因（承载面被遮 + StrictMode 从不触发）。
  - 推论:**难以肉眼判定的一次性/低频效果，先接探针再调体感**（`web/src/features/game/theme/telemetry.ts` 是现成范式）。
- 「干完了」到底指什么、交付说明该长什么样 → 见 **§9 完成的定义与交付格式**（本节只管**怎么说**，§9 管**说什么**）。

## 5. 视觉与体感判断权在 Felix

- **真机 / 浏览器冒烟由 Felix 拍板**，agent **不代做视觉体感判断**（「好不好看」「够不够明显」「像不像一记钟鸣」不是 agent 的裁量范围）。
- agent 该做的是**把可测的部分测掉并给读数**（透过率、opacity 是否全程不变、时序、布局是否溢出），把不可测的部分**明确交出去**。
- 冒烟需要 Felix 跑时，**给出可直接复制的命令文本**与「要看什么」的清单。

## 6. 花钱与对外动作，Felix 亲手

- **部署、注册、绑卡、`fly` 命令、任何产生费用或对外可见的动作，一律 Felix 亲手执行**；agent 只提供**命令文本**与判定标准。
- **API key 只进 `bakeoff/.env`（已 gitignore）**，绝不写进对话、代码、日志或提交。
- 真 key 冒烟会花钱：**先用 mock / 桩后端把能验的验完**，把真 key 留给只有它能验的事。

## 7. 文档口径单一真理源

- **同一条约定不得两处并存全文**：选一处当真理源，其余处放**指针**。
- 现行分工：**ADR 管「为什么」与架构决策** → **`AGENTS.md` 管「动手时不许越的线」（常驻可执行版）** → **`CLAUDE.md` / 设计稿放指针**。两处不一致时**以 ADR 为准**并回改常驻版。
- **旧 ADR 不改写**，由新 ADR 修订（ADR-017 修订 ADR-003、ADR-015 修订 ADR-002 都是这个形态）；历史文档保持原样，自新 ADR 起口径以其为准。
- **发现常驻读物过期就地订正** —— agent 常驻读物的过期状态比人读到危害大得多。

## 8. 范围纪律

- 每批 / 每刀的 brief 里若有「**不准顺手做**」清单，**逐条当硬线**；想做的顺手事写进交付说明当建议，不要直接做。
- 反过来，**发现的缺陷该修就修**，但要在交付说明里说明它为何属于本批范围（例：刀 2.5 的灵脉打磨是「孤立验收通过、真实页面里塌」的缺陷修复，不是范围膨胀）。
- **修一个立字时，凡与它同一失效形状的立字，同刀一起修**（Felix 2026-08-10 裁定，ADR-020 刀 5）。
  **分刀依据是「改动是否独立」，不是「是否在 brief 字面里」。**
  实例：刀 5 把《寻常》的密度表从描述改成机制，而 §8「收束阶段气力不得低于 15」**同样以「人生阶段」为条件**——
  留给下一刀意味着时钟修好了它仍不生效，下次冒烟又收获一个「无法验」。
  ⚠️ 这条与上面那条「不准顺手做」**不矛盾，判据不同**：那条防的是**另一件事**被夹带进来，
  这条说的是**同一件事的另一半**不该被留在原地。拿不准时，在交付说明里单独列出并说明理由，由 Felix 裁。
- 挂账优先于顺手做：不做但值得记的，写进对应 backlog 或 ADR 的挂账表。

## 9. 完成的定义与交付格式

### 9.1 什么才算「完成」

相关测试全绿 **+** lint **+** build 全绿 **+** 该守的**零回归护城河**未破（后端 golden parity；前端未登记世界的 DOM 对拍，见 ADR-018 §4.5.1）。**少一样都不叫完成**——「除了 X 都过了」等于没过，如实写 X。

### 9.2 交付说明必含七项（**不得只写「done」**）

每次交活按下面七项交代，缺项就明写「无」。目的很直接：**让审查者不必反问就能判断能不能合并**。

| 项 | 写什么 | 反例 |
|---|---|---|
| **摘要** | 做了什么、**为什么这么做**（关键取舍一句话说清） | 「实现了需求」 |
| **改动文件** | 动了哪些文件 / 哪几个 commit，各自一句话职责 | 只贴 diff 行数 |
| **已跑测试与结果** | 命令 + **具体数字**（`mvn test` 312 绿 / `npm test` 177 绿 / lint / build），零回归护城河是否验过 | 「测试通过」 |
| **人工检查** | agent 自己**实测到的读数**（透过率、opacity、时序、布局尺寸、对拍校验和），以及**哪些必须 Felix 亲自看** | 「看起来没问题」 |
| **已知风险** | 可能出问题的地方、**本地验不了的部分及其原因** | 略过不提 |
| **假设** | 依赖了哪些未经确认的前提（若前提错，哪部分要重做） | 默认前提成立 |
| **挂账项** | 发现但本批不做的，去了哪个 backlog / ADR 挂账表 | 「以后再说」 |

**与 §4「如实报」的关系**：§4 是态度（一是一二是二），本节是**格式**——固定格式的用处在于**漏项会显形**：写不出「人工检查」通常意味着没实测，写不出「已知风险」通常意味着没想过边界。

**篇幅按批次大小走**：一行 CSS 的修复不必写七段，但**七项都要有着落**（可以合并成两三句）；反过来，动了共享路径 / 引擎 / schema 的批次，七项一项都不能省。

---

# Motion Constraints（前端视觉/动效常驻约束）

> 真理之源 = **[ADR-017](docs/adr/ADR-017-frontend-visual-charter-and-animation-libraries.md)**（前端视觉宪法与动画库许可名单，修订 ADR-003）。本节是其**常驻可执行版**（lockstep 副本）：ADR 管「为什么」，本节管「动手时不许越的线」。两处不一致时**以 ADR-017 为准**并回改本节。
>
> **分工立字**：**GSAP Skill 管技术正确性**（API 怎么用才对、怎么清理、怎么不掉帧）；**AGENTS.md 与 ADR 管审美边界与架构约束**（做不做、做几个、谁给谁让路）。**冲突时项目 ADR 优先。**

## 0. 视觉宪法（ADR-017 §2，援引用）

1. **内容永远第一，任何动画不能影响阅读**（唯一的一票否决项）。
2. **动画不是为了炫，而是为了表达。**
3. **每个世界都有自己的视觉语言**——玩家不看标题就知道进了哪个世界。
4. **追求记忆点，不追求炫**——「世界会变化，而不仅仅是页面会动画」。

优先级配比：**氛围沉浸 50% > 交互反馈 35% > 关键时刻 15%**。反面清单：不做「AI PPT 审美」（紫色玻璃拟态、霓虹渐变堆砌、无意义通用炫技）。

## 1. 每个世界只有一个一级记忆点（唯一，不得增设）

| 世界 | 一级记忆点 |
|---|---|
| 修仙 `cultivation` | **钟鸣共振** bell resonance |
| 规则怪谈 `rules_creepy` | **灯闪** light flicker |
| 克苏鲁 `cthulhu` | **文字异常/不确定** uncertain text anomaly |
| 末日 `apocalypse` | **电台信号** radio transmission |

- **一级 = 该世界的签名动作，一个世界只准有一个。**想让某世界「再多一个记忆点」时，**答案是加强现有那个，不是并列第二个**——并列即稀释，四个世界各自加一点，最后四个世界长得一样。
- 融合世界不新立一级记忆点：**host 世界的记忆点打底 + 渗漏元素点缀**（同 ADR-012「host 优先 + 语义换皮」）。
- 与 ADR-017 §6.3 第一版三点的关系：规则怪谈灯闪 / 克苏鲁文字异常逐条对应；修仙 = **钟鸣共振**（初版「境界跳变整页照亮」被判为「看过就忘」的通用升级语言，已由「一声钟鸣」取代，**§6.3 已就地订正**；照亮若保留只作钟鸣的伴随表现，不另立一级）；末日电台为 B 批新增。

## 2. 动效预算（每屏硬上限）

**任一时刻同一屏最多**：

- **1 个持续环境动效**（呼吸感 / 云雾 / 沙尘一类，长期在跑的）
- **+ 1 个低频偶发动效**（灯闪 / 远光扫过 / 电台杂讯一类，隔一阵来一下的）
- **+ 1 个用户触发动效**（按压 / 数值滚动 / 规则点亮一类，玩家动作的回应）

超出即撞原则 ①。**预算是硬的**：想加第二个持续环境，就得先关掉现有那个。

### 2.1 互斥的两种表现 = 一个槽（Felix 2026-07-27 批准，附三条防滥用）

**判据：槽位管的是「同时运行的效果数」，不是「效果种类数」。** 两种表现若**永不同时发生**，记作**一个槽**。
先例：修仙天穹层的流云 + 微粒同层合并算一个持续槽；末日的电台 + 远光共用一个低频槽。

**三条防滥用，缺一即算超预算**：

1. **互斥必须是代码保证**（真的有一道硬闸判断，如「电台忙碌期一律跳过远光」），**不是「概率上很少同时发生」**；
2. 代码里**显式声明**共用槽位与互斥关系（写在效果所在文件的头注释里，让下一个人一眼看见）；
3. 动效预算表记作**一个低频槽**，并**标注两种表现 + 互斥机制**——不许在表里悄悄写成两条。

## 3. 新增动效必须声明它替代谁或降级谁

任何新效果的提案/实现说明里必须有一句：**「它替代 X」或「它把 X 降级为 Y」**——**不得只做加法**。

原因：护栏若只写在单轮任务指令里，下一轮新需求一来即失效——修仙再加剑影、规则怪谈再加故障、克苏鲁再加扭曲、末日再加沙尘爆闪，每一步都局部合理，合起来就是四个世界一起变成霓虹灯。**加法要付账**，账就是这一句。

## 4. 正文即禁区

- **叙事正文永不持续动画**（不呼吸、不抖动、不循环发光；一次性的进入/揭示动画做完即止）。
- **高亮与粒子进入正文区前必须淡出**——灰尘/孢子/灰烬飘到正文上方即淡出，绝不压在字上。

## 5. 生命周期与清理（技术硬线）

- **所有 `timeline` / `delayedCall` / `Observer` / 事件监听必须在卸载时清理**（React 侧优先 `gsap.context()` + `ctx.revert()`，见 `.agents/skills/gsap-react/`）。
- **重复输入不得堆叠 timeline**：同一触发再次到来时，**先 kill/复用既有 timeline 再起新的**（或直接 `gsap.quickTo` 一类可复用件）。玩家连点选项不该攒出十条并行时间线。
- **必须实现 reduced-motion 降级**：`prefers-reduced-motion: reduce` 下动效降到最简或直接关掉（`gsap.matchMedia()` 的 `reduceMotion` 条件）。现有前端已全局尊重该媒体查询，新增效果不得开天窗。

## 6. 性能

- **优先 `transform` 与 `opacity`**（`x`/`y`/`scale`/`rotation`/`autoAlpha`）；**避免动画化布局属性**（`width`/`height`/`top`/`left`/`margin`/`padding`）。
- 真实风险面是**低端 Android 与微信内置 WebView**（ADR-017 已知代价 2）：动效越重越挑设备，不达标就降级或退回 CSS。

## 7. 库许可（ADR-017 §3，白名单制）

**白名单（2026-07-29 收窄后，两个）**：**GSAP**（时间线编排，**在用**）/ **Lenis**（平滑滚动，尚未使用）。
~~**Motion**~~ **已移出**（见下）。
**缓引不引**：Particles / Lottie / 轻量 Shader。**Three.js 明确不做。**
**白名单之外的任何库（含缓引清单转正、含已移出的 Motion）须回 Project 窗口对齐，不得在实现批自行引入。**

**白名单是许可不是义务**（Felix 2026-07-25 立字）：ADR-017 批的库是「**允许在需要时用**」，不是「要把它们都用上」。刀 2 的玉简签按压用 CSS `:active` + 时间感变量做，与 Motion 的 `whileTap` 等价——**为一个按压反馈引入整个库是纯债**。

**✅ 这条立字已兑现一次（2026-07-29，ADR-017 §3.1 修订自身）**：基础世界视觉移植**四刀走完，Motion 一次未用**——四世界的按压／进出场／数值滚动全由 CSS `:active` + `--t-dur`/`--t-ease` 令牌与通用层 GSAP 插值做成；克苏鲁那处「每次按压时长微随机」是唯一 CSS 单做不到的点，解法是**三行 JS 写一个 CSS 变量**。故 **Motion 移出白名单**，移植债一篮子从三个库降为两个（实际只装了 GSAP）。**留复议口子**（同 `@gsap/react` 口径）：将来若有 Motion 真正不可替代的场景，**凭真实需求**回窗口申请，**无证据的便利驱动不构成理由**。

**Lenis 不一起砍**：它的适用场景（滚动质感）四刀**根本没碰**，属「未验证」而非「已证明不需要」——**收窄要建立在证据上**。

**减债习惯**：能用 CSS 达到的效果优先用 CSS；库只用在 CSS 明显不划算处（时间线编排、可中断的连续插值）。引的每个库都是回国做小程序时视觉层重做的债。

## 8. GSAP Skill 的通用建议里，本项目不适用的部分

`.agents/skills/gsap-*` 是上游 GreenSock 原样拷贝（见 [`.agents/skills/GSAP-VENDOR.md`](.agents/skills/GSAP-VENDOR.md)），面向通用 web 场景。以下建议**本项目 ADR 优先，该建议不适用**：

| Skill 里的建议 | 本项目口径 |
|---|---|
| 推荐 **ScrollTrigger** 做滚动驱动动画（`gsap-core` / `gsap-timeline` / `gsap-performance` / `gsap-react` 多处提及） | **不适用。非真正滚动驱动的效果不得引入 ScrollTrigger。** 本项目是屏内状态型叙事界面，动效由**状态变化**驱动而非滚动位置；`gsap-scrolltrigger` skill 已刻意不装 |
| `gsap-react` §Installation 让 `npm install @gsap/react`（`useGSAP()` 钩子） | **已裁定驳回（2026-07-23，Felix）。本项目不引 `@gsap/react`，一律走退路方案**：`useEffect` 内 `gsap.context()` + 清理里 `ctx.revert()`（同一 skill 自带的写法）。**理由**：① 它是白名单三库之外的**第四个 npm 包**，ADR-017 白名单**墨迹未干即破例**，护栏严肃性受损——「白名单可随时加一个」的先例代价远大于多写几行；② `useGSAP()` 的实际价值（自动清理 + 作用域绑定）`gsap.context()` + `ctx.revert()` **全都有**，只是啰嗦几行。**留的口子**：落地段（移植回 `web/src`）时若 `gsap.context()` 在真实 React 生命周期里**确有严重痛点**，凭**真实证据**回 Project 窗口申请——**无证据的便利驱动不构成理由** |
| `gsap-core` 指向 **`gsap-plugins`**（Flip / Draggable / MorphSVG…）与 **CustomEase** | 同包插件（`registerPlugin`）**不算新依赖、无移植债增量**，但仍受**动效预算**与原则 ② 约束：用之前要能回答「它服务哪个原则、替代了谁」。ScrollTrigger 是明确禁项，不在此列 |
| `gsap-core`「**Prefer GSAP Instead of CSS Animations**」 | **反过来**：ADR-017 §4 减债习惯 = **能用 CSS 的优先 CSS**，GSAP 留给时间线编排与连续插值 |
| `gsap-core`「未指定库时**默认推荐 GSAP**」 | 本项目**分工已定**：React 声明式交互（按压/进出场/数值滚动）归 **Motion**，GSAP 只做时间线编排——不因 skill 的默认推荐把 Motion 的活挪给 GSAP |
| `gsap-performance` 的 `will-change` / ScrollTrigger 性能节 | `will-change` 只给真正在动的元素（skill 自己也这么说）；ScrollTrigger 相关整节不适用 |
